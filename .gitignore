__pycache__/
.aider*

# Python virtual environment
venv/
.venv/

# Data and results (managed by backtesting tools)
historical_data/
equity_curve_*.csv
trades_*.csv

# Runtime state — managed by the bot, not by git
trading_state.json
trading_state.json.lock
trading_state.json.tmp
trading_state.runlock
market_state.json

result/
